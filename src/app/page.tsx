// This file effectively replaces the original App.js by setting up the main page structure.
// All core logic is now within AppShell and its child components/hooks.
import { AppShell } from '@/components/layout/app-shell';

export default function SeatingSagePage() {
  return <AppShell />;
}
